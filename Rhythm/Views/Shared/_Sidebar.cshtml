@*@model Rhythm.Models.RecentViewModel.RecentContentViewModel*@

<!-- Sidebar -->
<div class="col-sm-4 col-md-3 col-md-offset-1 sidebar">
    <!-- TODO: Search Widget -->
    <div class="widget">
        @using (Html.BeginForm("Index", "Search", FormMethod.Post, new { @class = "form-inline form", @role = "form" }))
        {
            @Html.AntiForgeryToken()
            <div class="search-wrap">
                <button class="search-button animate" type="submit" title="Start Search">
                    <i class="fa fa-search"></i>
                </button>
                @Html.TextBox("searchText", "", new { @class = "form-control search-field", @placeholder = "Search..." })
            </div>
        }
    </div>
    <!-- End Search Widget -->
    <div class="widget">
        @*<h5 class="widget-title font-alt">Categories</h5>
            <div class="widget-body" id="recentCategories">
                <ul class="clearlist widget-menu">
                    @foreach (var category in @Model.CategoryRecentViewModel)
                    {
                        <li>
                            <a href="@Url.Action("Category", "Search", new { id = category.Id, Name = category.Name })" title="@category.Name">@category.Name</a>
                            <small>
                                - @category.CountCategory
                            </small>
                            <div class="fa fa-examples">
                                <i class="fa fa-fw">&#xf0a5</i>
                            </div>
                        </li>

                    }
                </ul>
            </div>*@
        @Html.Action("RecentCategories", "Recents")
    </div>
    <!-- End Widget -->
    <div class="widget">
        @*<h5 class="widget-title font-alt">Tags</h5>
            <div class="widget-body">
                <div class="tags">
                    @foreach (var tag in Model.TagRecentViewModel)
                    {
                        <a href="@Url.Action("Tag", "Search", new { id = tag.Id, Name = tag.Name })" title="@tag.Name">@tag.Name</a>
                        @Html.Hidden(tag.Name)
                    }
                </div>
            </div>*@
        @Html.Action("RecentTags", "Recents")
    </div>
    <!-- End Widget -->
    <div class="widget">
        @*<h5 class="widget-title font-alt">Last posts</h5>
            <div class="widget-body">
                <ul class="clearlist widget-posts">
                    @foreach (var post in Model.PostRecentViewModel)
                    {
                        <li class="clearfix">
                            <div class="widget-posts-descr">
                                <a href="@Url.Action("Post","Posts", new { id = post.Id})" title="@post.Title">@post.Title</a>
                                Posted by @post.NameSenderPost in @post.PostedOn
                            </div>
                            <div class="fa fa-examples">
                                <i class="fa fa-fw">&#xf0f6</i>
                                <i class="fa fa-fw">&#xf0f6</i>
                                <i class="fa fa-fw">&#xf0f6</i>
                            </div>
                        </li>
                    }
                </ul>
            </div>*@
        @Html.Action("RecentPosts", "Recents")
    </div>
    <!-- End Widget -->
    <div class="widget">
        @*
                <h5 class="widget-title font-alt">Comments</h5>
                        <div class="widget-body">
                <ul class="clearlist widget-comments">
                    @foreach (var comment in Model.CommentRecentViewModel)
                    {
                        <li>
                            @comment.NameUserSender on <a href="@Url.Action("Post","Posts", new { id = comment.Id } )" title="@comment.CommentContent">@comment.CommentContent (@comment.PostAddedDate)</a>
                            <div class="fa fa-examples">
                                <i class="fa fa-fw">&#xf0e5</i>
                                <i class="fa fa-fw">&#xf0e5</i>
                                <i class="fa fa-fw">&#xf0e5</i>
                            </div>
                        </li>

                    }
                </ul>
            </div>*@
        @Html.Action("RecentComments", "Recents")
    </div>
    <!-- End Widget -->
    <!-- TODO: Widget Articles-->
    <div class="widget">
        @*<h5 class="widget-title font-alt">Text widget</h5>
            <div class="widget-body">
                <div class="widget-text clearfix">
                    @if (Model.ArticleWidgetViewModel.ImageData != null)
                    {
                        <img width="200" height="133" src="@Url.Action("GetImage", "Posts", new { Model.ArticleWidgetViewModel.Id })" />
                    }
                    <br /><br />
                    <h2 class="blog-item-title">
                        @Html.ActionLink(Model.ArticleWidgetViewModel.Title, "Post", "Posts", new { id = Model.ArticleWidgetViewModel.Id }, new { @class = "" })
                    </h2>
                    @Html.Raw(Model.ArticleWidgetViewModel.ArticleContent)
                </div>
            </div>*@

        @Html.Action("RecentArticleWidgets", "Recents")
    </div>
    <!-- End Widget -->
    <!-- TODO: Widget Archove-->
    <div class="widget">
        @Html.Action("RecentArchives", "Recents")
    </div>
    <!-- End Widget -->
</div>
<!-- End Sidebar -->