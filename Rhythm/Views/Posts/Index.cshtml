@model IEnumerable<Rhythm.Domain.Model.Post>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@foreach (var item in Model)
{
    <tr>
        @Html.ActionLink("Details", "Details", new { id = item.ID }, new { @class = "mn-has-sub" })
    </tr>

}